<section class="section">
  <div class="container">
    <div class="columns">
        <%=render 'feed' %>
        <%=render 'who-to-follow'%>
        <%=render 'trends'%>
    </div>
</section>

<%= link_to 'New Tweet', new_tweet_path %>
